{
  "entities": {
    "classes": {
      "title": "School Classes",
      "description": "Classes and student rosters shared across all physical education teachers",
      "type": "object",
      "properties": {
        "className": { "type": "string", "description": "The display name of the class" },
        "studentCount": { "type": "number", "description": "Total number of students in the class" },
        "students": { "type": "array", "description": "List of student objects with numeroEleve, nomEleve, sexe, photoUrl" },
        "physicalTests": { "type": "array", "description": "Latest recorded physical test scores and biometric measurements" },
        "vmaResults": { "type": "array", "description": "Latest recorded Luc Léger VMA test results" },
        "updatedAt": { "type": "string", "description": "ISO timestamp of the last modification" },
        "updatedBy": { "type": "string", "description": "Author or device that synced the class" }
      },
      "required": ["className", "studentCount", "students", "updatedAt"]
    },
    "physical_tests": {
      "title": "Physical Tests",
      "description": "Physical tests and biometric evaluations shared for each class",
      "type": "object",
      "properties": {
        "className": { "type": "string", "description": "The name of the class" },
        "results": { "type": "array", "description": "List of physical test records" },
        "updatedAt": { "type": "string", "description": "ISO timestamp of the last modification" }
      },
      "required": ["className", "results", "updatedAt"]
    },
    "vma_results": {
      "title": "VMA Results",
      "description": "Luc Léger VMA test results shared for each class",
      "type": "object",
      "properties": {
        "className": { "type": "string", "description": "The name of the class" },
        "results": { "type": "array", "description": "List of VMA results records" },
        "updatedAt": { "type": "string", "description": "ISO timestamp of the last modification" }
      },
      "required": ["className", "results", "updatedAt"]
    },
    "archives": {
      "title": "Data Archives",
      "description": "Archived school years and student rosters snapshots with tests",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier of the archive" },
        "title": { "type": "string", "description": "Title of the archive, e.g. الموسم الدراسي 2024/2025" },
        "description": { "type": "string", "description": "Optional notes or remarks" },
        "createdAt": { "type": "string", "description": "Creation ISO date" },
        "createdBy": { "type": "string", "description": "Teacher name or device" },
        "classCount": { "type": "number", "description": "Number of classes archived" },
        "studentCount": { "type": "number", "description": "Total students in archive" },
        "data": { "type": "object", "description": "Complete snapshot of classes, physical tests, and VMA results" }
      },
      "required": ["id", "title", "createdAt", "classCount", "studentCount"]
    }
  },
  "firestore": {
    "/classes/{classId}": {
      "schema": "classes",
      "description": "Shared student lists per class"
    },
    "/physical_tests/{classId}": {
      "schema": "physical_tests",
      "description": "Shared physical tests results"
    },
    "/vma_results/{classId}": {
      "schema": "vma_results",
      "description": "Shared VMA results"
    },
    "/archives/{archiveId}": {
      "schema": "archives",
      "description": "Archived historical seasons and student records"
    }
  }
}
